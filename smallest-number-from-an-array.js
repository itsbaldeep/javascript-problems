// Let an array of some random numbers
const array = [12, 13, 14, 11, 15];

// Using the spread operator(...)
// We pass in the elements as arguments in min method of Math object
console.log(Math.min(...array));

// It logs the smallest number from the array
